export const increment = () => ({
    type: "INCREMENT",

  });
  
  export const decrement = () => ({
    type: "DECREMENT",
  });
  
  export const setLoading = (loading) => ({
    type: "SET_LOADING",
    loading,
  });
